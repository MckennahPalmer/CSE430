# 1.3.1.1.5 Register Classes:

```
registerClasses(query) : tuple
  # Maybe we can take something easier to read than a string, maybe and object Query?
  
  queryParts = query.split(",")
  
  action = queryParts[0]
  element = queryParts[1]
  data = queryParts[2]  # Can we assume that this data is compatible with Jenzabar?
  token = queryParts[3]
  
  if action == "PUT" and element == "classes":
  	response = request.put("https://jenzabar.com/api/register", token, data)
    
  return (response.status, response.data)
```